collections_config:
  pages:
    path: ''
    name: pages
    label: Pages
    icon: wysiwyg
    filter:
      include:
        - index.html
    _enabled_editors:
      - content

data_config:
  gallery:
    path: _data/gallery.yml

_inputs:
  src:
    type: image
    label: Photo
    uploads_dir: photos/Bentley_files
  alt:
    type: text
    label: Photo Title
  category:
    type: select
    label: Category
    options:
      values:
        - exterior
        - interior

uploads_dir: photos/Bentley_files
